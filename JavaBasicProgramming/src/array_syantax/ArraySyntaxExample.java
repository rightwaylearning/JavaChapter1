package array_syantax;

public class ArraySyntaxExample {
	public static void main(String[] args) {
		byte b = 10;
		float[] arr = new float['A'];
		// here size is int type but u can use upcasting way 
		 // like u can use, char, byte, short
		System.out.println(arr.length);
		
		
		arr[0] = 10;  // upcasing
		arr[1] = 'B';
		
		// u can use upcasting 
		// & downcation in array  (size, array element data init)
		

		
	}
}


// for code format  >>> first do contr + A  ,  contr + shift + F
// cntr + z  >> back   
// cntr + y  >> forward